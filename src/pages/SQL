i have learnt SQL Select INTO,
INSERT INTO,
SELECT STATEMENT AND ALSO USING THE * keyword inorder to Select all the columns in the table,
CREATE DB,
DROP DB,
BACKUP DB,
CREATE TABLE, 
ALTER TABLE,
DROP TABLE,
CONSTRAINT,
NULL,
UNIQUE,
PRIMARY KEY & FOREIGN KEY,
DEFAULT,
CHECK,
WHERE STATEMENT
